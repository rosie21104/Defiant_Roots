repos:
  - repo: local
    hooks:
      - id: semgrep
        name: Semgrep Security Scan
        entry: .venv/bin/semgrep --config auto
        language: system
        types: [python]
        pass_filenames: false
